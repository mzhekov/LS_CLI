# Test dependencies for LeadSauce CLI

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Optional testing utilities
pytest-xdist>=3.3.1          # Parallel test execution
pytest-timeout>=2.1.0         # Test timeouts
pytest-asyncio>=0.21.0        # Async test support

# Mocking and fixtures
mock>=5.1.0
faker>=19.2.0                 # Generate fake data for tests

# Coverage reporting
coverage[toml]>=7.2.7

# Code quality (optional)
pytest-flake8>=1.1.1         # Linting in tests
pytest-mypy>=0.10.3          # Type checking in tests

# Database testing
pytest-postgresql>=5.0.0     # PostgreSQL testing (if needed)

# Rich/Console testing utilities
# (already included in main requirements)
